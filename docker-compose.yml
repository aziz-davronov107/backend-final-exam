version: "3.9"

services:
  db:
    image: postgres:15
    container_name: azizbek_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12345
      POSTGRES_DB: fixoo
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: azizbek_redis
    restart: always
    ports:
      - "6380:6379"

  app:
    build: .
    container_name: azizbek
    restart: always
    env_file:
      - .env
    ports:
      - "1170:1170"
    depends_on:
      - db
      - redis  
    command: >
      sh -c "
        npx prisma generate &&
        npx prisma migrate deploy &&
        npm run start:prod
      "
    environment:
      DATABASE_URL: postgres://postgres:12345@db:5432/fixoo
      REDIS_HOST: redis
      REDIS_PORT: 6379

volumes:
  postgres_data:
